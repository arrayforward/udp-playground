# 多径重乱序·真实包络版（60s，循环执行）
# 与 reorder_heavy 的区别：乱序窗口压到端侧丢帧判定窗（60ms）以内。
# 背景：reorder_heavy（max 100ms）下 30% 报文迟到超 60ms 判丢窗，
# 帧在乱序分片到达前被判丢 → 丢帧/IDR 风暴（实测卡顿率 10.99%）。
# 真实多径（聚合/双路径）乱序通常 10~50ms——本场景在此包络内验证
# 保序逻辑：帧级保序窗口应完全吸收乱序，乱序不引发丢帧、卡顿率
# 应接近同条件无乱序基线。
# 行程：
# 1) 聚合重乱序（0-20s）——18M、30% 乱序 40ms：保序窗口主压测
# 2) 轻度乱序（20-40s）——15% 乱序 20ms：常规多径抖动
# 3) 聚合重乱序（40-60s）——回到 30%/40ms：复测稳定性
20: bw 18000000; loss 0.01; delay-normal 20 6 0.01 50 110; reorder-prob 0.3; reorder-max 40
20: bw 18000000; loss 0.01; delay-normal 20 6 0.01 50 110; reorder-prob 0.15; reorder-max 20
20: bw 18000000; loss 0.01; delay-normal 20 6 0.01 50 110; reorder-prob 0.3; reorder-max 40
